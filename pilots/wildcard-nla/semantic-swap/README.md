# Experiment 4: semantic prefix swap

Keeps each sealed Experiment 3 final token clause and replaces the first two clauses with the next prompt in the same 8-way group. No new AV generation.

Frozen protocol: `PROTOCOL.md`. Runner: `code/nla/run_semantic_swap_study.py`.
